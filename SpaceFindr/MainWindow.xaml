<Window x:Class="SpaceFindr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpaceFindr"
        mc:Ignorable="d"
        Title="SpaceFindr ALPHA" Height="900" Width="1600"
        SizeChanged="Window_SizeChanged">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="DrivesPanel" Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Left" Margin="0,0,0,10" />
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="BrowseButton" Content="Browse Folder" Width="150" Height="40" Margin="0,0,10,0" Click="BrowseButton_Click"/>
                <Button x:Name="BackButton" Content="Back" Width="80" Height="40" Margin="0,0,10,0" Click="BackButton_Click" Visibility="Collapsed"/>
                <StackPanel Orientation="Horizontal" x:Name="ScanningPanel" Margin="10,0,0,0" Visibility="Collapsed">
                    <ProgressBar IsIndeterminate="True" Width="32" Height="32" Margin="0,0,8,0"/>
                    <Label Content="Scanning..." VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel x:Name="BreadcrumbPanel" Orientation="Horizontal" VerticalAlignment="Center" Height="40" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2">
            <TabControl x:Name="MainTabControl" SelectionChanged="MainTabControl_SelectionChanged">
                <TabItem Header="Treemap">
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="6">
                        <Canvas x:Name="TreemapCanvas" Background="#FAFAFA"/>
                    </Border>
                </TabItem>
                <TabItem Header="Largest Files">
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="6">
                        <ListView x:Name="FilesListView" Margin="0" FontFamily="Consolas" FontSize="13">
                            <ListView.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Open" Click="File_Open_Click" />
                                    <MenuItem Header="Ask AI" Click="File_AskAI_Click" />
                                    <MenuItem Header="Delete" Click="File_Delete_Click" />
                                </ContextMenu>
                            </ListView.ContextMenu>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="File Path" DisplayMemberBinding="{Binding FullPath}" Width="350"/>
                                    <GridViewColumn Header="Size" DisplayMemberBinding="{Binding SizeDisplay}" Width="100"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Border>
                </TabItem>
                <TabItem Header="Settings">
                    <StackPanel Margin="16">
                        <CheckBox x:Name="ShowFreeSpaceCheckBox" Content="Show free space on drives" IsChecked="True" Checked="ShowFreeSpaceCheckBox_Checked" Unchecked="ShowFreeSpaceCheckBox_Unchecked" />
                    </StackPanel>
                </TabItem>
            </TabControl>
        </Grid>
        <StatusBar Grid.Row="3" Height="28" VerticalAlignment="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="CurrentFolderText" Text="Current folder: " />
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="FolderFileCountText" Text="Items: 0 folders, 0 files" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock>
                    <Hyperlink NavigateUri="https://popeen.com" RequestNavigate="Hyperlink_RequestNavigate">popeen.com</Hyperlink>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
