<Window x:Class="SpaceFindr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpaceFindr"
        mc:Ignorable="d"
        Title="SpaceFindr ALPHA" Height="900" Width="1600"
        SizeChanged="Window_SizeChanged">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/> <!-- Footer row -->
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="BrowseButton" Content="Browse Folder" Width="150" Height="40" Margin="0,0,10,0" Click="BrowseButton_Click"/>
                <Button x:Name="BackButton" Content="Back" Width="80" Height="40" Margin="0,0,10,0" Click="BackButton_Click" Visibility="Collapsed"/>
                <StackPanel x:Name="BreadcrumbPanel" Orientation="Horizontal" VerticalAlignment="Center" Height="40" />
            </StackPanel>
            <StackPanel Orientation="Vertical" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,0,0">
                <TextBlock x:Name="DriveUsageName" FontWeight="Normal" FontSize="13" Margin="0,0,0,2"/>
                <ProgressBar x:Name="DriveUsageBar" Width="180" Height="18" VerticalAlignment="Center" Minimum="0" Maximum="100" Margin="0,0,0,0"/>
                <TextBlock x:Name="DriveUsageText" VerticalAlignment="Center" Margin="0,4,0,0" TextAlignment="Left"/>
            </StackPanel>
        </Grid>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10" Visibility="Collapsed" x:Name="ScanningPanel">
            <ProgressBar IsIndeterminate="True" Width="150" Height="20" Margin="0,0,10,0"/>
            <Label Content="Scanning..." VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" CornerRadius="6">
            <Canvas x:Name="TreemapCanvas" Background="#FAFAFA"/>
        </Border>
        <!-- Footer -->
        <StatusBar Grid.Row="3" Height="28" VerticalAlignment="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="CurrentFolderText" Text="Current folder: " />
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="FolderFileCountText" Text="Items: 0 folders, 0 files" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
